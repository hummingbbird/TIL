# 😧 오늘의 TIL
오늘은 그리디 알고리즘을 공부한다. 책을 통해 학습 후, 배운 내용을 간단히 정리한다.

<br><br>

## 💡 Greedy: 당장 좋은 것만 선택하는 알고리즘

- 단순하지만 가장 강력한 문제 해결 방법
- 특정 문제를 만났을 때, 단순히 현재 상화엥서 가장 좋아 보이는 것만을 선택해도 문제를 풀 수 있는지 파악할 수 있는지가 중요
- 문제에서 '가장 큰 순서대로', '가장 작은 순서대로'와 같은 기준을 은밀하게 제시해줌 .. 오 ..
- 자주 정렬 알고리즘과 짝을 이뤄 출제됨
- 가장 기본이면서 대표 문제: 동전 거스름돈


<br><br>

## 📍 예제1: 큰 수의 법칙
> 2019 국가 교육기관 코딩 테스트 기출 문제

<br>

## 문제 설명

'큰 수의 법칙'은 일반적으로 통계 분야에서 다루어지는 내용이지만 동빈이는 본인만의 방식으로 다르게 사용하고 있다. 동빈이의 큰 수의 법칙은 다양한 수로 이루어진 배열이 있을 때 주어진 수들을 M번 더하여 가장 큰 수를 만드는 법칙이다. 단, 배열의 특정한 인덱스(번호)에 해당하는 수가 연속해서 K번을 초과하여 더해질 수 없는 것이 이 법칙의 특징이다.

예를 들어 순서대로 `2, 4, 5, 4, 6`으로 이루어진 배열이 있을 때 M이 8이고, K가 3이라고 가정하자. 이 경우 특정한 인덱스의 수가 연속해서 세 번까지만 더해질 수 있으므로 큰 수의 법칙에 따른 결과는 6 + 6 + 6 + 5 + 6 + 6 + 6 + 5인 46이 된다.

단, 서로 다른 인덱스에 해당하는 수가 같은 경우에도 서로 다른 것으로 간주한다. 예를 들어 순서대로 `3, 4, 3, 4, 3`으로 이루어진 배열이 있을 때 M이 7이고, K가 2라고 가정하자. 이 경우 두 번째 원소에 해당하는 4와 네 번째 원소에 해당하는 4를 번갈아 두 번씩 더하는 것이 가능하다. 결과적으로 `4 + 4 + 4 + 4 + 4 + 4 + 4`인 28이 도출된다.

배열의 크기 N, 숫자가 더해지는 횟수 M, 그리고 K가 주어질 때 동빈이의 큰 수의 법칙에 따른 결과를 출력하시오.

### 입력 조건

* 첫째 줄에 $N(2 \le N \le 1,000)$, $M(1 \le M \le 10,000)$, $K(1 \le K \le 10,000)$의 자연수가 주어지며, 각 자연수는 공백으로 구분한다.
* 둘째 줄에 $N$개의 자연수가 주어진다. 각 자연수는 공백으로 구분한다. 단, 각각의 자연수는 1 이상 10,000 이하의 수로 주어진다.
* 입력으로 주어지는 $K$는 항상 $M$보다 작거나 같다.

### 출력 조건

* 첫째 줄에 동빈이의 큰 수의 법칙에 따라 더해진 답을 출력한다.

## 입출력 예

| 입력 (N, M, K / 배열) | 출력 |
| --- | --- |
| `5 8 3` <br> `2 4 5 4 6`| `46` |

<br><br>

## 💡 접근 방법 및 풀이 과정

N(숫자 개수) M(번 더함) K(번 초과하여 더할 수 없음)

2 4 5 4 6
M: 8 k: 3

숫자 배열에서 8개를 뽑는데, 이 때 연속해서 3번까지만 더할 수 있다는 뜻

예제를 보면서 생각해보니 그냥 가장 큰 두개의 숫자만 사용해주면 된다는 생각이 들었다. (어차피 그거보다 작은 애들을 쓸 일이 없음) 또한, N의 범위를 보니 배열의 최소 길이는 2이기 때문에 아래와 같은 방법으로 해결할 수 있었다.

> 어차피 444 3 444 3 ..과 같은 패턴의 반복
> 
> 즉, 가장 큰 수를 쭉 더하고 연속 횟수가 k만 넘지 않게 해주면 된다.

이걸 코드로 옮기니 바로 정답~! 근데 이게 탐욕법이 맞아요? 큼. 일단 이어서 보자.

## 💡 그래서 답은

```python
N, M, K = map(int, input().split())
nums = list(map(int, input().split()))
answer = 0

nums.sort()
first, second = nums[-1], nums[-2]

cnt = 0

for _ in range(M):
    if cnt == K:
        cnt = 0
        answer += second
    else:
        cnt += 1
        answer += first

print(answer)
        
```

## 🤔 근데 이거 .. 이렇게 풀어도 되나?

해설도 읽어보고 생각도 해보니 반복문을 쓰지 않고도 풀 수 있을 것 같았다. 패턴을 계산식으로 표현하여 저장한 후, 각 숫자를 그 횟수만큼만 반복해서 더해주면 된다. 핵심은 '**두 번째로 큰 숫자의 개수**' 

두 번째로 큰 숫자의 개수를 `second_cnt`라고 한다면

M <= K 일 때는, K가 몇이든 상관없이 0이다.(`second_cnt = 0`)

else -> `second_cnt = M // (K+1)`

그럼 answer = (second_cnt * second) + ((M-second_cnt) * first) 와 같이 반복문 없이도 문제를 해결할 수 있다 ~

```python
N, M, K = map(int, input().split())
nums = list(map(int, input().split()))

nums.sort()
first = nums[-1]
second = nums[-2]

second_cnt = M // (K + 1)
first_cnt = M - second_cnt

answer = (first_cnt * first) + (second_cnt * second)
print(answer)

```

# 😧 끝마치며

저 코테공부에 쓰는 시간을 조금 줄여야해서 당분간 조금씩만 공부하겠습니더 .. ^^